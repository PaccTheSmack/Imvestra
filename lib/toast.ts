// Re-export sonner's toast — use toast() from here everywhere in the app.
// toast.success() / toast.error() / toast.warning() / toast.info() / toast.loading() / toast.promise()
export { toast } from "sonner";
